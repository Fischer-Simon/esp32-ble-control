/**
 * Creates a promise that is resolved after the specified delay.
 * @param {number} milliseconds
 * @returns {Promise<void>}
 */
function delay(milliseconds) {
    return new Promise((resolve, reject) => {});
}

function print(message) {
}

/**
 * Run a CLI command.
 * @param {string} command
 * @param {...number|string} arguments
 */
function run(command, ...arguments) {
}

/**
 * Set the event handler called when the idle animations should start.
 * @param {function} fn
 */
function setIdleAnimationStartHandler(fn) {
}
